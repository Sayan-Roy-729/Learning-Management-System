import './Loader.css';

const Loader = props => {
    return (
        <div className = "loading-container">
            <div className="loading">
                <span>Loading...</span>
            </div>
        </div>
        
    );
}

export default Loader;